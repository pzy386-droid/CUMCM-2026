# 发给 fable 的提示词

请在保留现有 Q1/Q2/Q3 的基础上完成第四问。先合入 `origin/codex/q4-handoff`，完整阅读 `docs/q4/README.md` 所列原题、附件、融合方案，以及本目录模型与验收文档；本交接分支只提供材料和规格，生产代码以你现有 `codex/q3-implementation` 为起点，建立 `codex/q4-implementation`。

主路线已经锁定：**波动价下分别重优化 Q2 A 和 Q3 F2，采用同历史日负载/光伏/价格联合情景、共享因果储能反馈、物理投影、跨日SOC和唯一账本。**

关键要求：

1. Q4-2：每天0时联合优化全天固定合同与反馈规则，继承Q2 A、不做日内重解、不使用附件3。Q4-3：0时原合同冻结，6/12/18时用已发布光伏预报调整未来合同和规则，保留同情景候选保护。附件3权限歧义按README另做敏感性，不能暗中改变主版。
2. 未来实际价不能提前读取。主价预测lag-1，成熟的历史同日三元残差至多8条、源日全天固定；第三储能特征是按历史尺度归一化的当前价格偏差。价格必须进入合同、应急、调整及尾部目标系数，不得只对旧策略换价计费。未来价已知版独立命名，非主结果。
3. 结算逐段使用附件4实际价：4-2为p(g+5e)；4-3为p[g+1.5(a−g)+ +0.5(g−a)+ +5e]。原g0不变，a是交付时有效合同；按最终a相对g0结算一次。不要把更新时刻单价广播至全部未来区间。
4. 先修复复用组件的尾部失败关闭：非法SOC、矩阵残差、NaN、无可行解或模式违规不能进入guard评分；违规尾部整数重试后仍失败就保留旧策略。三维特征必须同时覆盖规划、guard、执行和快照回放，不能只改feature_dim。
5. 物理继承原规格：十分钟乘1/6，充放各0.9、5000kW、S为1200—10800kWh；从1/1的6000连续回放，不在二月或每天复位。尾部6000只是虚拟估值终值，不进入实际账单。
6. 按 `implementation_and_acceptance.md` 先通过收费手算、未来数据扰动、价格响应人工例、尾部故障注入、版本冻结、模板读回，再做连续20日预检并锁定预算；随后串行跑两条正式全年run，再做同一真实价格环境的对照。不要先跑全年再核对题意，不为更好的指定日拼接结果。
7. 生成 `result4-2.xlsx`、`result4-3.xlsx`，334天全部结果及四指定日表1—3；表头修正与最终合同含义按规格。Excel、表、图、summary必须同一账本。报告现金费用分项、库存、gap、回退和价格信息假设，不能承诺一定降费或全局最优。
8. 分批提交，正式源码干净后运行；Git提交代码/配置/测试/文档/小报告，完整账本和快照另打包并给逐文件及归档SHA。push失败就交付bundle与实际可下载归档，不能只给清单或说文件在对方电脑。

正常实现和已规定关卡通过后自行继续；出现题面直接矛盾时给原文证据与反例，不擅自改模型口径或审查预期。最终明确列出主结果、对照、测试、失败处理、未完成事项和实际交付位置。

# 发给 fable 的执行提示词

请在你已有的 `codex/q2-implementation`（包含c10be27）上创建 `codex/q3-implementation`，fetch远端后合入本仓库标签 `q3-handoff-v1`，完成第三问。本标签仅含交接材料，不含Q2实现。先完整阅读原题、附件和 `docs/q3/README.md` 指向的全部规格，以及 `reports/q2/astra_review.md`。原题优先，规格中的主解释不能冒称题目明确规定。不要照搬Q2旧消融结论。

主方案：已发布光伏预报＋同历史源日的联合残差情景＋共享低维储能反馈＋发布时滚动改约。0时用附件3本次预报优化原合同g并冻结；6/12/18时只用当时信息，固定真实SOC与g，联合优化剩余合同a和储能规则。日内固定历史源日，更新对应预报版本残差，不用Q2的最近邻重选。每十分钟按已生效合同物理投影执行，全年SOC连续。

关键点：6时第1小时预报对应7时；6时只能改第37段以后；kW除6得kWh；反馈误差统一相对0时基准。主费用 `Σp*g+Σp[1.5(a-g)^++0.5(g-a)^+]+5Σp*e`，最终a相对g每段只结算一次，主策略a>=g；退款版本单独重优化。新、旧候选须在同一当前情景/S/尾部上经投影比较，不能用真实未来挑赢家。本版采用阶段内继续执行至日末的近似，不冒称实现了原方案的未来仿射改约或多阶段全局最优。

严格按 `acceptance_and_experiments.md`：手算收费/时间映射/未来扰动/冻结测试→小模型与Q1/Q2回归→连续20日及跨年边界预检→提交干净代码、固定配置→单进程全年→独立核验与完整归档。预检通过后自行继续；发现实质题意冲突给原文、反例和解决方案，不能修改审查预期来让测试通过。不要同时跑多个全年任务抢核心。

交付同一run生成的result3四表、四指定日表1–3、图、费用分解、完整账本/合同与规则快照、求解日志/元数据/哈希，以及预报、控制、改单价值的公平消融和“是否需要其他时刻预报”分析。Excel调整表填最终a，不填增减量；过去区间冻结；18时不预设无价值；不存在的新预报不编造收益。源码与小报告正常push，完整运行归档必须实际交到双方可访问处；push失败交bundle及可获取归档，不只说文件在你机器。保留Q1/Q2结果，不直接改main，不force push。
